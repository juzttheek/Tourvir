---
interface Props { id: string; label: string; required?: boolean; class?: string }
const { id, label, required = false, class: className = 'form-group' } = Astro.props;
---
<div class={className}><label class="form-label" for={id}>{label}{required && <span aria-hidden="true"> *</span>}</label><slot /></div>
