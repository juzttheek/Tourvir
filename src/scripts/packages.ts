/* ============================================
   Tourvir — Packages Behaviors Entry Point
   ============================================ */

import { initPackagesFilter } from './packages-filter.js';

export function initPackages(): void {
  initPackagesFilter();
}
