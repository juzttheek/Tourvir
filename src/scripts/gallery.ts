/* ============================================
   Tourvir — Gallery Behaviors Entry Point
   ============================================ */

import { initGalleryController } from './gallery-controller.js';

export function initGallery(): void {
  initGalleryController();
}
