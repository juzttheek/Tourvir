{
  "name": "tourvir",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25",
    "npm": ">=11"
  },
  "scripts": {
    "dev": "node scripts/serve-static.mjs . 4173",
    "preview": "node scripts/serve-static.mjs dist 4173",
    "build": "node scripts/build-static.mjs",
    "format:check": "prettier --check .",
    "lint:js": "eslint js scripts tests *.config.*",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint": "npm run lint:js && npm run lint:css",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run tests/unit",
    "test:contract": "vitest run tests/contract",
    "test:e2e": "npm run build && playwright test tests/e2e",
    "test:a11y": "npm run build && playwright test tests/accessibility --workers=1",
    "test:visual": "npm run build && playwright test tests/visual",
    "test:lighthouse": "npm run build && node scripts/run-lighthouse.mjs",
    "check:html": "html-validate \"*.html\"",
    "check:links": "node scripts/check-links.mjs",
    "check:content": "node scripts/check-content.mjs",
    "validate": "npm run format:check && npm run lint && npm run typecheck && npm run test:unit && npm run test:contract && npm run check:html && npm run check:links && npm run check:content && npm run build && npm run test:e2e && npm run test:a11y && npm run test:visual && npm run test:lighthouse"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.0",
    "@types/node": "24.10.1",
    "astro": "7.1.3",
    "eslint": "10.8.0",
    "globals": "17.3.0",
    "html-validate": "11.5.6",
    "jsdom": "29.1.1",
    "lighthouse": "12.6.1",
    "prettier": "3.9.6",
    "stylelint": "17.14.1",
    "stylelint-config-standard": "40.0.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
